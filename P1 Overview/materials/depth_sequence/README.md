# Panel d measured-frame placeholders

Add six measured depth-map images here:

- `depth_000.png`
- `depth_001.png`
- `depth_002.png`
- `depth_003.png`
- `depth_004.png`
- `depth_005.png`

Use consecutive frames from a hemispherical indenter pressing into the
elastomer, captured at 400 FPS. Keep the field of view, depth colour range and
spatial calibration identical across frames. Add the calibrated scale-bar
length once the real sequence is selected.

